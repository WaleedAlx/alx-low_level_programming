#include "main.h"
#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: prtiny the result for mul two numbers
 *
 * Return: Always 0 (success)
 */
int main(void)
{
    printf("%d\n", mul(98, 1024));
    printf("%d\n", mul(-402, 4096));
    return (0);
}
