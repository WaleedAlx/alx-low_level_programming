#include "main.h"
/**
 * mul - claculates the multiplication of two numbers
 * @a: first int parameter number
 * @b: second int parameter number
 * Return: The result of two numbers multiplicattion
 */

int mul(int a, int b)
{
	return (a * b);
}
