#include "main.h"

/**
 * _abs - Calcualtes the absolute value of an integer
 *
 * @n: the int passed number to check
 *
 * Return: absolute value of int
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
