#include "main.h"
/**
 * _islower - Function to chec for lowercase character
 * @c: the character to be checked
 * Return: 1 if the character is lowercase, 0 Otherwise
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
