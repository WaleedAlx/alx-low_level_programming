#include "main.h"
/**
 * _abs - Function to compute the absolute value of an integer
 * @n: The int parameter to check
 * Return: The absolute value of integer
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
