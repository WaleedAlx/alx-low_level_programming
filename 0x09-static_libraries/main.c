#include "main.h"
#include <stdio.h>
/**
 * main - Entry point
 * Description: Test _puts function
 *
 * Return: Always 0 (success)
*/
int main(void)
{
	_puts("At the end of the day, my goal was to be the best");
	return (0);
}
