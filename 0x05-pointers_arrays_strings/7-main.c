#include "main.h"

/**
 * main - Entry point
 * Description: Testing puts_half function
 * Return: Always 0 (success)
 */
int main(void)
{
	char *str;

	str = "0123456789";
	puts_half(str);
	return (0);
}
